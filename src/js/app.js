(function() {
	'use strict';

	angular
		.module('demo', ['angular-google-map-v2'])
		.config(config)
		.run(run);

	/** @ngInject */
	function config($provide) {

	}

	/** @ngInject */
	function run($rootScope) {

	}

})();
